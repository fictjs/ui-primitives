# @fictjs/progress

Accessible progress primitives for Fict UI primitives, modeled after `@radix-ui/react-progress`.

Exports `createProgressScope`, `Progress`, `ProgressIndicator`, `Root`, and `Indicator`.
