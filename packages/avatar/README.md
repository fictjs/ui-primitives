# @fictjs/avatar

Accessible avatar primitives for Fict UI primitives, modeled after `@radix-ui/react-avatar`.

Exports `createAvatarScope`, `Avatar`, `AvatarImage`, `AvatarFallback`, `Root`, `Image`, and `Fallback`.
