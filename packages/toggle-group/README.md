# @fictjs/toggle-group

Toggle group primitives for Fict UI primitives, modeled after
`@radix-ui/react-toggle-group`.

Exports `createToggleGroupScope`, `ToggleGroup`, `ToggleGroupItem`, and the
matching `Root` / `Item` aliases.
