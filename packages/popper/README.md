# @fictjs/popper

Popper positioning primitives for Fict UI primitives, modeled after
`@radix-ui/react-popper`.

Exports `createPopperScope`, `Popper`, `PopperAnchor`, `PopperContent`,
`PopperArrow`, and the matching `Root` / `Anchor` / `Content` / `Arrow` aliases.
