# @fictjs/slider

Slider primitives for Fict UI primitives, modeled after
`@radix-ui/react-slider`.

Exports `createSliderScope`, `Slider`, `SliderTrack`, `SliderRange`,
`SliderThumb`, and the matching `Root` / `Track` / `Range` / `Thumb` aliases.
