# @fictjs/tabs

Tabs primitives for Fict UI primitives, modeled after
`@radix-ui/react-tabs`.

Exports `createTabsScope`, `Tabs`, `TabsList`, `TabsTrigger`, `TabsContent`,
and the matching `Root` / `List` / `Trigger` / `Content` aliases.
