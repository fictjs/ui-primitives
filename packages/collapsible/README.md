# @fictjs/collapsible

Accessible collapsible primitives for Fict UI primitives, modeled after `@radix-ui/react-collapsible`.

Exports `createCollapsibleScope`, `Collapsible`, `CollapsibleTrigger`, `CollapsibleContent`, and the matching `Root`, `Trigger`, and `Content` aliases.
