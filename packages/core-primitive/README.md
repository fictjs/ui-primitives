# @fictjs/core-primitive

Internal DOM and event utilities for Fict UI primitives, modeled after `@radix-ui/primitive`.

Exports `canUseDOM`, `composeEventHandlers`, `getOwnerWindow`, `getOwnerDocument`, `getActiveElement`, and `isFrame`.
