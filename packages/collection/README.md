# @fictjs/collection

Collection registration primitives for Fict UI primitives, modeled after `@radix-ui/react-collection`.

Exports the stable `createCollection` API from the legacy Radix-compatible implementation, plus `unstable_createCollection` and `unstable_CollectionProps` for the newer ordered-dictionary variant used by packages like one-time password field.
