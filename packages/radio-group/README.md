# @fictjs/radio-group

Radio group primitives for Fict UI primitives, modeled after
`@radix-ui/react-radio-group`.

Exports `createRadioGroupScope`, `RadioGroup`, `RadioGroupItem`,
`RadioGroupIndicator`, and the matching `Root` / `Item` / `Indicator` aliases.
