# @fictjs/compose-refs

Fict ref composition helpers modeled after `@radix-ui/react-compose-refs`.

## Installation

`pnpm add @fictjs/compose-refs`

## API

- `composeRefs(...refs)`
- `useComposedRefs(...refs)`

Both helpers accept callback refs and ref objects and return a single callback ref.
