# @fictjs/primitives-preview

Local preview site for the Fict UI primitives workspace.

Run:

```bash
pnpm preview:dev
```

Build:

```bash
pnpm preview:build
```

The site is wired against the local workspace packages so the demos reflect the
current repository state.
